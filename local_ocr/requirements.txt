# Flask and WSGI server
Flask==3.1.0
gunicorn==23.0.0

# PyTorch (CPU-only version - GPU version installed separately in Dockerfile.gpu)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# Image processing
Pillow>=10.0.0

# PDF processing
PyMuPDF>=1.24.0

# Numpy
numpy>=1.26.0

# Surya OCR (~300M params) - CPU and GPU friendly
surya-ocr>=0.8.0

# PaddleOCR-VL (via transformers) - SOTA document parsing
# Requires transformers 5.0+ for native PaddleOCR-VL support
transformers>=5.0.0rc1
accelerate>=0.34.0
