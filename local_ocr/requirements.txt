# Flask and WSGI server
Flask==3.1.0
gunicorn==23.0.0

# PyTorch (CPU-only version, compatible with numpy 2.x)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

# Transformers for PaddleOCR-VL model (needs recent version for use_kernel_forward_from_hub)
transformers>=4.46.0

# Image processing
Pillow>=10.0.0

# PDF processing
PyMuPDF>=1.24.0

# Hugging Face Hub for model downloads
huggingface-hub>=0.20.0

# Additional dependencies that may be needed
einops
accelerate
safetensors
sentencepiece

# Numpy - let pip resolve compatible version
numpy>=1.26.0
